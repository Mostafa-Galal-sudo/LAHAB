# Local Worker secrets. Copy to .dev.vars and replace every placeholder.
ADMIN_JWT_SECRET=replace-with-a-long-random-secret
RESEND_API_KEY=

# Fine-grained GitHub token: repository access to Mostafa-Galal-sudo/LAHAB,
# with Contents permission set to Read and write. Never expose this in VITE_*.
GITHUB_ASSET_TOKEN=github_pat_replace_me
